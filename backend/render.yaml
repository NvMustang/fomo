services:
  - type: web
    name: fomo-backend
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
    # Ajoutez vos autres variables d'environnement ici

